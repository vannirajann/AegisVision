# Copy this file to ".env" once Person 5's backend is running,
# then fill in the real addresses. Vite only reads variables that
# start with VITE_.

VITE_API_BASE_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/ws/alerts
